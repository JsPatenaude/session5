
public class Server {
	int port;
	String ip;
	
	public Server() {
		this.port = 5000;
		this.ip = "0.0.0.0";
	}
	
	public Server(int port, String ip) {
		this.setPort(port);
		this.setIp(ip);
	}
	
	public int getPort() {
		return this.port;
	}

	// Set port if valid
	// Return true if valid port, or false if not valid
	public boolean setPort(int port) {
		if (port >= 5000 && port <= 5050) {
			this.port = port;
			return true;
		}
		return false;
	}
	
	public String getIp() {
		return this.ip;
	}

	// Set ip address if valid
	// Return true if valid ip address, or false if not valid
	public boolean setIp(String ip) {
		final String IP_PATTERN = "^((0|1\\d?\\d?|2[0-4]?\\d?|25[0-5]?|[3-9]\\d?)\\.){3}(0|1\\d?\\d?|2[0-4]?\\d?|25[0-5]?|[3-9]\\d?)$";
		if (ip.matches(IP_PATTERN)) {
			this.ip = ip;
			return true;
		}
		return false;
	}

	
}
